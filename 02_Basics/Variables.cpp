#include <iostream>

/**
 * @brief Regeln für Variblenname:
 *
 * 1.) Can not start with a number -> int 0my_variable
 * 2.) Can not contain whitespaces -> int 0my variable
 * 3.) Variable names need to/have to be unique.
 *
 * @return int
 */


int main()
{
    int my_integer_value = 2; // besser so als Deklaration und Zuweisung getrennt zu haben.
    float my_float_value = 4.5;

    return 0;
}
